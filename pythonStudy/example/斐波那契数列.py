"""
斐波那契数列
前两位只和等于第三位数字

"""
a = 0
b = 1
for _ in range(20):
    # a = b
    # b = a + b

    # 下边的赋值运算同上边的赋值运算不同；上边的是先将b赋值给a a的值是1；然后运算a+b b的值是2
    # 下边的是先赋值 相当于 a,b=1,1
    a, b = b, a + b

    print(a, end=' ')
